spring:
  cloud:
    nacos:
      discovery:
        namespace: somersault-cloud-dev
        # local本地开发模式默认关闭Nacos注册和配置，按需开启调试
        enabled: false
      config:
        namespace: somersault-cloud-dev
        # local本地开发模式默认关闭Nacos注册和配置，按需开启调试
        enabled: false